## Installation

Inside the root directory run:

pip install -e .

## Run examples

python -m examples.file_name

## Run tests

Inside the `tests` directory run:

python -m pytest py_test.py -q
